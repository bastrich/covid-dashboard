version: '3.9'

services:

  postgres:
    image: yhpark90/covid-dashboard-postgres:v1
    environment:
      POSTGRES_USER: covid
      POSTGRES_PASSWORD: covid
      POSTGRES_DB: covid
    ports:
      - 5432:5432